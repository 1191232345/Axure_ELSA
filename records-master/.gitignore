# =============================================================================
# records-master Git 提交忽略说明
# =============================================================================
# 本文件定义不应提交到 Git 的内容，主要包括：
#   1. 数据库文件（db）—— 含评价记录、员工等业务数据
#   2. 环境变量与密钥（.env）—— 含 SECRET_KEY 等敏感配置
#   3. 运行时产物 —— 日志、缓存、虚拟环境等
#   4. 本地工具/工作流文档 —— 非项目交付代码
#
# 注意：若文件已被 Git 跟踪，添加忽略规则后需执行：
#   git rm -r --cached <文件或目录>
#   然后再提交，才能从仓库中移除（本地文件仍保留）。
# =============================================================================

# --- 数据库（db）与业务数据 ---
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-wal
*.db-shm
data/
db/
data copy/

# --- 环境变量与密钥（保留 .env.example 作为模板）---
.env
.env.local
.env.*.local

# --- Python 运行时 ---
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
.eggs/
dist/
build/

# --- 日志 ---
logs/
*.log
app.log

# --- 系统与编辑器 ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# --- 本地导入/备份数据（可能含真实员工信息）---
py/employees.csv
py/*.bak
*.backup

# --- AI 工作流报告（非项目代码）---
.trae/
